log:
  debug: true
apis:
  coap:
    address: "0.0.0.0:5684"
    external-port: 5684
    fqdn: "localhost"
    request-timeout: 10s
    heartbeat: 4s
    max-message-size: 1024  # default:262144
    keepalive-enabled: true
    keepalive-timeout: 20s
    blockwise-transfer-enabled: true
    blockwise-transfer-block-size: 1024
    # tcp-signal-message-csm-disabled : false
    # peer-tcp-signal-message-csm-disabled : false
    # error-response-enabled : true
    tls:
      enabled: true
      ca-file: "/data/certs/root_ca.crt"
      dir-path: "/data/certs"
      cert-file: "coap.crt"
      key-file: "coap.key"
      client-certificate-required: true
clients:
  oauth-provider:
    oauth:
      client-id: ""
      client-secret: ""
      scopes: []
      audience: ""
      endpoint:
        token-url : ""
      request-timeout : 10s
    tls:
      enabled: true
      ca-file: "/data/certs/root_ca.crt"
      dir-path: "/data/certs"
      cert-file: "http.crt"
      key-file: "http.key"
      use-system-ca-pool: true
  authorization:
    address: "127.0.0.1:9081"
    tls:
      enabled: true
      ca-file: "/data/certs/root_ca.crt"
      dir-path: "/data/certs"
      cert-file: "http.crt"
      key-file: "http.key"
      use-system-ca-pool: true
  resource-directory:
    address: "127.0.0.1:9082"
    tls:
      enabled: true
      ca-file: "/data/certs/root_ca.crt"
      dir-path: "/data/certs"
      cert-file: "http.crt"
      key-file: "http.key"
      use-system-ca-pool: true
  resource-aggregate:
    address: "127.0.0.1:9083"
    tls:
      enabled: true
      ca-file: "/data/certs/root_ca.crt"
      dir-path: "/data/certs"
      cert-file: "http.crt"
      key-file: "http.key"
      use-system-ca-pool: true
