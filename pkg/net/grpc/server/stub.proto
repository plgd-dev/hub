syntax = "proto3";

package ocf.cloud.test.pb.v2;

option go_package = "github.com/plgd-dev/cloud/v2/pkg/net/grpc/server";

service StubService {
  rpc TestCall(TestRequest) returns (TestResponse) {}
  rpc TestStream(stream TestRequest) returns (stream TestResponse) {}
}

message TestRequest {
    string test = 1;
}

message TestResponse {
    string test = 1;
}
