{{- if .Values.certificateauthority.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "plgd-hub.certificateauthority.fullname" . }}
  namespace: {{ .Release.Namespace }}  
  labels:
    {{- include "plgd-hub.labels" . | nindent 4 }}
    {{- with .Values.certificateauthority.service.labels }}
    {{- . | toYaml | nindent 4 }}
    {{- end }}
  {{- if .Values.certificateauthority.service.annotations }}
  annotations:
    {{- include "plgd-hub.tplvalues.render" ( dict "value" .Values.certificateauthority.service.annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.certificateauthority.service.type | default "ClusterIP" }}
  ports:
    {{- if .Values.certificateauthority.service.ports }}
    {{- toYaml .Values.certificateauthority.service.ports | nindent 4 }}
    {{- else }}
    - port: {{ .Values.certificateauthority.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    {{- end }}
  selector:
  {{- include "plgd-hub.certificateauthority.selectorLabels" . | nindent 4 }}
{{- end }}